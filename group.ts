export class Group {
  group: string[];

  
  public addToGroups(move: string) {
    // this.groups.forEach((g) => {

    // })

    
  }

  public foundAjacent(item: string) {
    for (let i = 0; i < this.group.length; i++) {
      if(this.group.includes(item)) {
        
      }
      
    }
  }
}